#front
  .row
    -if ((Selection.all.length > 0) && Election.find(Selection.all[0].eid))
      .span2#selectionred
        = Election.find(Selection.all[0].eid).name
        %br
        = Election.find(Selection.all[0].eid).day.strftime("%B %-d, %Y")
    -else
      .span2#selectionred
        No election event selected.
    .span8
      %table{:style=>"width:100%;"}
        %tr#statebar
          %td#doing{:style=>"width:25%;"}
            Select
          %td#todo{:style=>"width:25%;"}
            Add Logs
          %td#todo{:style=>"width:25%;"}
            Add Voters
          %td#todo{:style=>"width:25%;"}
            Analyze
      %hr
  .row
    .span2.sidebar
      %p
        Select an election event that you or colleagues 
        have already created. After selecting one, you 
        can update it with additional log data if 
        any, and proceed to the analytics of all the log 
        data that describes each voter’s activities 
        during this election cycle.
    .span10
      #likeh2blue Election Events
      #scroil{:style=>"height: 325px;"}
        %table{:style=>"text-align:center;margin:10px;"}
          %tr
            %th Election
            %th &nbsp;
            %th Election
            %th &nbsp;
            %th Voter Start
            %th &nbsp;
            %th Voter End
            %th &nbsp;
            %th Voter
            %th 
            %th
            %th
            %th
          %tr
            %th Name
            %th &nbsp;
            %th Date
            %th &nbsp;
            %th Date
            %th &nbsp;
            %th Date
            %th &nbsp;
            %th Logs
            %th 
            %th
            %th
            %th
          - @elections.each do |election|
            %tr
              %td= election.name
              %td
              %td= election.day
              %td
              %td= election.voter_start_day
              %td
              %td= election.voter_end_day
              %td
              %td= election.voter_transaction_logs.length
              %td{:style=>"font-weight:bold;"}= (((Selection.all.length > 0) and (election.id == Selection.all[0].eid)) ? "&lowast;".html_safe : "" )
              %td= link_to 'Select', {:select => true, :id => election.id}
              %td= link_to 'Show', election
              %td= link_to 'Destroy', election, :confirm => 'Are you sure?', :method => :delete
  .row#positionbacknext
    .span2
      &nbsp;
    .span1#statebacknext
      = link_to 'Back', :root
    .span6
      &nbsp;
    -if ((Selection.all.length > 0) and (Election.all.any? {|e| e.id == Selection.all[0].eid}))
      .span1#statebacknext
        = link_to "Next", '/elections/'+Selection.all[0].eid.to_s
